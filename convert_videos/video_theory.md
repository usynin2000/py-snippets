### 1️⃣ Видео — это не просто картинка

Картинка — это набор точек (пикселей), каждая точка имеет цвет.

Видео — это много картинок подряд.
Например, 50 кадров в секунду (50 fps) — значит, за 1 секунду показано 50 отдельных изображений.

Иллюстрация:
```shell
Кадр 1: 🟥⬛🟦
Кадр 2: 🟥⬛🟦 (почти такой же)
Кадр 3: 🟥⬛🟦 (немного поменялся)

```

Каждый кадр — это «фотография», а видео — это 50 фотографий в секунду.


### 2️⃣ Почему видео весит так много

Представь, что каждое изображение — это таблица чисел (цвет каждого пикселя).

Для 4K видео (3840x2160) один кадр — это миллионы пикселей.

Если хранить все кадры полностью, даже 10 секунд видео займут гигабайты.


Пример:

3840 × 2160 пикселей × 3 цвета (RGB) × 8 бит на цвет ≈ 24 МБ на кадр

50 fps → 1 секунда = 50 × 24 МБ ≈ 1,2 ГБ!

10 секунд = 12 ГБ — вот почему видео без сжатия гигантское.


### 3️⃣ Кодеки — «умные упаковщики»

Видео сжимают, чтобы оно занимало меньше места.

Кодек — это правила упаковки.

Вместо того чтобы хранить каждый кадр полностью, кодек хранит только изменения между кадрами.

Если на видео фон не меняется, кодек просто говорит: «оставь всё как было» → экономия места.

```shell
Кадр 1: полный
Кадр 2: фон не менялся, двигается только человек
Кодек хранит только движение человека, фон оставляет прежним
```


### 4️⃣ Цвет и битность — простая аналогия

- 8-бит = 256 оттенков цвета
- 10-бит = 1024 оттенка цвета

Пример:

- 8-бит: плавный градиент может «ступенчато» отображаться (banding)

- 10-бит: градиент плавный, лучше для цветокоррекции

- 4:2:0, 4:2:2 — это как «срезали» половину информации о цвете, чтобы экономить место.

    - 4:2:0 = меньше данных → файлы легче

    - 4:2:2 = больше данных → лучше цвет


### 5️⃣ Разница в размере файла

Исходное видео 500 МБ: почти без сжатия, 10-бит, 4:2:2

Перекодированное 25 МБ: 8-бит, 4:2:0, H.264 → сильно сжатое

Внешне почти одинаково, но внутри «меньше данных», меньше цветов и деталей, которых глаз может не заметить.


### 6️⃣ Почему бесплатный DaVinci не читает видео

Бесплатная версия не понимает «сложные» кодеки (10-бит, 4:2:2)

Она умеет работать только с обычным H.264 8-бит 4:2:0

Поэтому приходится перекодировать видео, чтобы редактор понял формат




## H.264 — что это такое

H.264 — это кодек, то есть «умный упаковщик» видео.

Его задача — хранить видео в меньшем размере, но так, чтобы глаз почти не заметил разницу.

Работает по принципу «сохраняем не каждый кадр полностью, а только изменения между кадрами».

Примеры «магии» H.264:

Если фон на видео не меняется, кодек не хранит фон заново, а просто фиксирует движущиеся объекты.

Использует предсказание движения и компрессию цвета, чтобы сильно экономить место.

В результате видео с H.264:

Много меньше по весу, чем «сырое» (raw) видео.

Всё ещё достаточно качественное для YouTube, монтажа и т.д.

Широко совместимо: почти все плееры и монтажные программы умеют его читать (особенно 8-бит 4:2:0).


## 4:2:0 — что это такое

Это уже про цвет видео, точнее про подвыборку цвета (chroma subsampling).

Почему это нужно

Наш глаз более чувствителен к яркости (luma) чем к цвету (chroma).

Можно хранить меньше информации о цвете, почти не теряя видимого качества.

Как читается «4:2:0»

4:2:0 — это сокращённая запись того, сколько информации о цвете хранится на пиксели.

Расшифровка:


| Показатель | Значение                                                           |
| ---------- | ------------------------------------------------------------------ |
| 4          | количество яркостных (luma) сэмплов на первую строку из 4 пикселей |
| 2          | количество цветовых сэмплов по горизонтали                         |
| 0          | количество цветовых сэмплов по вертикали                           |


💡 Проще: на 4 пикселя по горизонтали берётся вся информация о яркости, а о цвете — только половина по горизонтали и половина по вертикали.


На глаз почти не видно потери цвета, зато размер файла сильно меньше.


Есть ещё варианты:

4:4:4 — полная информация о цвете (каждый пиксель полностью), большой размер

4:2:2 — половина цвета по горизонтали, полная по вертикали (часто для профессионального монтажа)

4:2:0 — половина цвета по горизонтали и вертикали (обычно для YouTube, бытового видео)


🔹 Связь с DaVinci и перекодировкой

Бесплатная версия DaVinci не умеет читать 10-бит 4:2:2 напрямую.

H.264 8-бит 4:2:0 — стандарт, который понимает почти любая программа.

Поэтому перекодировка через ffmpeg делает видео «дружелюбным» для монтажа.


## Разбор команды
```shell
ffmpeg -i C0001.MP4 -c:v libx264 -pix_fmt yuv420p -c:a aac -movflags +faststart output.mp4
```
1️⃣ ffmpeg

Это сам инструмент для работы с видео и аудио.
Он умеет: конвертировать, менять кодеки, обрезать, склеивать, накладывать фильтры и т.д.


2️⃣ -i C0001.MP4

-i — input (входной файл).
C0001.MP4 — это ваш исходный файл.
FFmpeg всегда сначала указывает входной файл после -i.


3️⃣ -c:v libx264

-c:v — выбрать кодек для видео (v значит video).
libx264 — это H.264, стандартный, широко совместимый кодек.
💡 Напомню: это именно «умный упаковщик», который сжимает видео, сохраняя визуально качество.


4️⃣ -pix_fmt yuv420p

-pix_fmt — pixel format (формат пикселей).
yuv420p — это 4:2:0, 8-бит, самый стандартный формат, который понимает почти любая программа, включая бесплатный DaVinci.
Именно это делает видео «дружелюбным» для монтажа в бесплатной версии.

5️⃣ -c:a aac
-c:a — выбрать кодек для аудио (a значит audio).
aac — популярный аудиокодек, который почти везде поддерживается (iPhone, YouTube, плееры).


6️⃣ -movflags +faststart

Это важно для онлайн-воспроизведения.
Файл MP4 устроен так, что информация о видео может находиться в конце файла.
+faststart перемещает таблицу с информацией о видео (moov atom) в начало файла, чтобы видео можно было начать смотреть в браузере до полной загрузки.


7️⃣ output.mp4

Это имя выходного файла, который создастся после перекодировки.

Он будет:
H.264 (libx264) видео
yuv420p (4:2:0) пиксели
AAC аудио
готов для онлайн-просмотра благодаря faststart


💡 Итого: команда берёт любое видео, делает его максимально совместимым и маленьким, но при этом глаз почти не заметит разницы.