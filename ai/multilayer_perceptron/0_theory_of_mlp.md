## Почему одного перцептрона мало
Сhat: https://chatgpt.com/c/696d0370-1a9c-8330-95f8-853a3c2d9b61 

Один перцептрон:

- считает взвешенную сумму
- проводит одну прямую / гиперплоскость
- говорит: по эту сторону — 1, по ту — 0

Но реальные задачи:
- нелинейные
- с условиями вида «и то, и это, но не третье»

👉 Значит, нам нужно несколько таких “линий”, а потом их как-то объединить.


### Идея MLP на пальцах:
> **Многослойный перцептрон** = перцептроны, сложенные слоями.


MLP — это не другой алгоритм,
это тот же самый перцептрон, но:
мы применяем его много раз подряд
и между **применениями искажаем пространство**

Входы → Скрытый слой → Выход


Каждый слой:
- берет числа
- считает взвешенную сумму
- применяет нелинейность
- передаёт дальше


Ключевое слово тут — **нелинейность.**

### Про нелинейность
Линейно

Если решение можно описать правилом:

> «если сумма признаков больше X → класс 1»

геометрически:
- прямая (2D)
- плоскость (3D)
- гиперплоскость

Перцептрон умеет только это.

#### Нелинейно

Если решение требует:
- «угол»
- «излом»
- «две области сразу»

пример XOR:
```shell
1 |  .   X
  |
0 |  X   .
    ------
      0   1

```

👉 одной прямой нельзя.

> Нелинейность — это способность “ломать” прямые линии.

Если каждый слой — только сложение,
то хоть 100 слоёв — всё равно прямая.

Поэтому:
> после суммы мы ОБЯЗАНЫ сделать нелинейное преобразование

# Слои и роли

1️⃣ Входной слой
- Это не “перцептроны”
- Просто числа (фичи)

x1, x2, x3

### Побольше про входной слой:
Очень просто:

> Входной слой — это вообще не нейроны



Это просто:

Никаких весов, активаций, логики.

Он:
- не учится
- не преобразует
- просто передаёт данные дальше


2️⃣ Скрытый слой (hidden layer)
Вот тут магия.

###  Что такое нейрон
Нейрон = маленький калькулятор

Он делает ровно 3 шага:
Шаг 1. Взвешенная сумма
```shell
z = w1*x1 + w2*x2 + b
```
Шаг 2. Активация (нелинейность)
```shell
a = f(z)
```

Шаг 3. Передаёт a дальше

👉 Вот это и есть нейрон. Ничего больше.


Каждый нейрон:
```
z = w1*x1 + w2*x2 + ... + b
a = activation(z)
```

Интуитивно:
- каждый нейрон учится простому признаку
- типа:
    - “x1 большой и x2 маленький”
    - “x1 и x2 одновременно равны 1”

### Как работает скрытый слой
Скрытый слой — это несколько нейронов параллельно.
```shell
x1 ─┬─▶ (●) ─▶ a1
x2 ─┼─▶ (●) ─▶ a2
    └─▶ (●) ─▶ a3
```

Каждый нейрон:
- смотрит на те же входы
- но с разными весами

👉 Значит, каждый ловит разный признак.

- нейрон 1: «x1 большой»
- нейрон 2: «x2 и x1 вместе»
- нейрон 3: «x1 − x2»

#### Что мы называем «признаком»
Очень важный момент:

> Признак — это не фича из датасета

Признак = результат работы нейрона
```shell
a = f(w·x + b)
```

Скрытый слой:
- создаёт новые признаки
- которых не было во входных данных

3️⃣ Выходной слой
- Собирает признаки
- Делает финальное решение

###  Как выходноц слой "собирает признаки"
Выходной слой — это тот же нейрон.

Он получает:
```shell
a1, a2, a3 ...
```

и делает:
```shell
y = f(v1*a1 + v2*a2 + b)
```
Интуитивно:
- скрытый слой: «что здесь вообще происходит»
- выходной слой: «ну и к какому классу это ближе»

4. Нелинейность — зачем она вообще нужна

👉 Он комбинирует признаки, а не входные данные напрямую.

Если убрать активации:
```shell
Linear → Linear → Linear = Linear
```

То есть:
сколько слоёв ни добавь — всё равно один перцептрон
Поэтому:
- sigmoid
- tanh
- ReLU (чаще всего сейчас)


Самая простая интуиция:

активация = “излом” линии

### Зачем нужны sigmoid / tanh / ReLU
Все они решают ОДНУ задачу:

> добавить нелинейность

Разница — как именно они это делают.
**sigmoid**
- сжимает в [0, 1]
- удобно как вероятность
- плохо обучается в глубоких сетях

**tanh**
- [-1, 1]
- центрирован около 0
- старый, но понятный

**ReLU**
```shell
f(x) = max(0, x)
```
- простая
- быстрая
- делает «излом»
👉 Без любой из них:
MLP превращается в один перцептрон

5. XOR — минимальный пример MLP

XOR решается 2 нейронами в скрытом слое.
```shell
x1 ──▶ (●) ─┐
            ├─▶ (●) → y
x2 ──▶ (●) ─┘

```

Интерпретация:

- первый нейрон ловит x1 OR x2
- второй — x1 AND x2
- выходной комбинирует


👉 Один слой учится разбивать пространство, другой — собирать куски.


## XOR и MLP — интуитивно, без формул

Один нейрон не может.
Но два могут:
Скрытый слой:
- нейрон 1: «x1 OR x2»
- нейрон 2: «x1 AND x2»

Выход:
- говорит:
- «если OR, но не AND → 1»

> 👉 Мы пересобрали пространство,
сделав XOR линейно разделимым.

6. Как MLP обучается (без ужаса)
Прямой проход (forward)

вход → слои → выход

Считаем ошибку
```python
loss(y_pred, y_true)
```

Обратное распространение ошибки (backprop)
- смотрим, кто сколько накосячил
- двигаем веса в сторону уменьшения ошибки


Важно:

> backprop — это просто аккуратное применение цепного правила, не магия

### Где тут backpropagation (пока очень грубо)
Пока просто запомни:
> backprop — это способ понять, какой вес испортил результат

Он:
- не меняет архитектуру
- не делает сеть умнее
- просто обновляет веса аккуратно

Мы к нему придём только после того,
как сеть станет для тебя «механической».


7. Чем MLP принципиально отличается от перцептрона

| Перцептрон            | MLP                                 |
| --------------------- | ----------------------------------- |
| 1 слой                | много слоёв                         |
| линейное разделение   | нелинейное                          |
| простые задачи        | почти любые табличные данные        |
| нет скрытых признаков | автоматическое извлечение признаков |


8. Ментальная модель (очень полезная)

Запомни вот это:
> Каждый слой — это смена системы координат

Сеть шаг за шагом:
- крутит пространство
- растягивает
- складывает
- пока классы не станут разделимы простой линией