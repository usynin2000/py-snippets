# Почему в случае ошибки, мы всегда меняем bias?


Короткий ответ

Мы всегда меняем bias, потому что:
> bias — это порог принятия решения,
и если модель ошиблась, возможно, вся граница решения стоит не там, даже если признаки ни при чём.

Интуиция на человеческом языке

Вспомни формулу:
```shell
результат = солнце * w_sun + дождь * w_rain + bias
```
- веса — аргументы
- bias — общее настроение / базовая склонность

Теперь представь:
«Я в целом сегодня не хочу гулять»

Это не связано ни с солнцем, ни с дождём.
Это смещение всей логики целиком.


#### Что происходит, если мы НЕ будем менять bias

Представим правило:

> «Если вообще ничего нет (0,0), я всегда ошибаюсь»
```shell
0*ws + 0*wr + bias → всегда одно и то же

```

Если bias зафиксирован —
👉 перцептрон физически не может исправить ошибку.

Он будет:
- бесконечно крутить веса
- но в точке (0,0) они не участвуют вообще
❗ Это ключевой момент.


Почему bias обновляется даже когда признаки = 0

Обновление:
```shell
bias += learning_rate * error
```

Обрати внимание:
- нет умножения на вход
- он реагирует на сам факт ошибки

Логика такая:

«Я в целом слишком оптимистичен / пессимистичен»


Конкретный пример (очень показательный)

Допустим, данные такие:

Солнце  Дождь → Гуляем
  0       0       1


Ошибка:

prediction = 0
error = 1

Что мы можем изменить?

- вес солнца? ❌ солнца нет

- вес дождя? ❌ дождя нет

👉 ОСТАЁТСЯ ТОЛЬКО BIAS

> И это не “на всякий случай”, это единственный возможный рычаг.


Геометрия (одна фраза, но важная)

- веса вращают линию
- bias двигает линию целиком

Если линия не там — вращение не спасёт.


- Почему мы меняем bias при ЛЮБОЙ ошибке

Ошибка означает:
«Граница решения в целом не там, где нужно»
Мы не знаем:

- виноваты ли конкретные признаки
- или виноват общий порог

Поэтому:
- веса корректируются пропорционально входам
- bias корректируется всегда

Это честный и простой алгоритм.


Очень важное резюме

- Bias — это вес признака, который всегда равен 1

Формально:
```shell
bias = w0 * 1
```

И тогда правило обновления абсолютно логично:
```shell
w0 += learning_rate * error * 1
```